{
  "private": true,
  "description": "Playwright visual-verification harness for the Logo Projection app. Run probes with: node modes.mjs <label> (server must be up on 127.0.0.1:5501 — use start.bat).",
  "dependencies": {
    "playwright": "^1.60.0"
  }
}
